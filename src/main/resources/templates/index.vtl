<!DOCTYPE html>
<html>
  <head>
    <title>Index</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <!--It's useful to put a comment at the top of your vtl file to help you keep track of what is what when things get more complex.-->
    <h1 style="text-align:center">To Do List</h1>
    #if (!$todos.isEmpty())
      <ul>
        #foreach( $todo in $todos )
          <li>$todo.content</li>
        #end
      </ul>
  #else
    <p>Nothing to do!</p>
  #end
  <form style="text-align:center" action="/todos/new" method="GET">
    <input type="Submit" value="Add new">
  </form>
  #if (!$todos.isEmpty())
    <form style="text-align:center" action="/todos/delete" method="GET">
      <input type="Submit" value="Delete all">
    </form>
  #end
  </body>
</html>