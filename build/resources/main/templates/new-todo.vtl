<!DOCTYPE html>
<html>
  <head>
    <title>Index</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <!--It's useful to put a comment at the top of your vtl file to help you keep track of what is what when things get more complex.-->
    <h1 style="text-align:center">To Do List</h1>
      <form style="text-align:center" action="/todos" method="POST">
        <input type="text" name="content">
        <span style="display:inline-block">
          <label for="labels">Label:</label>
           #if (!$labels.isEmpty())
              <select id="labels" name="labels">
                #foreach( $label in $labels )
                  <option value=$label.id>$label.name</option>
                #end
              </select>
            #else
              <p>No labels!</p>
            #end
          </span>
        <input type="Submit" value="Add new">
      </form>
  </body>
</html>